# AGENTS.md

## Startup deste projeto (obrigatório)

Sempre que iniciar sessão neste workspace:

1. Ler `docs/RUNBOOK_LOCAL.md`
2. Seguir exatamente os comandos de subida local
3. Validar as URLs principais (`/`, `/produtos`, `/quem-somos`, `/contato`)

## Regra de operação

- Não alterar rotas sem pedido explícito do Leandro.
- Fluxo oficial: GitHub -> EasyPanel.
- Se houver inconsistência local, restaurar com:
  - `git reset --hard origin/main`
  - `git clean -fd`

## Contexto do usuário

- Usuário: Leandro
- Projeto: Titanium Implantes
- Preferência: manutenção contínua, operação local confiável e validação antes de qualquer alteração.
